server:
  port: 8081
spring:
  application:
    name: view-user-service
    # mysql 属性配置
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/view?characterEncoding=utf8
    username: root
    password:
# mybatis mapper xml 配置
mybatis:
  mapper-locations: classpath:mybatis/*.xml

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8088/eureka/