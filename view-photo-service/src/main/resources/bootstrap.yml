server:
  port: 8082
spring:
  application:
    name: view-photo-service, datasource
  cloud:
    config:
      label: master
      profile: dev
      uri: http://localhost:8888/
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest